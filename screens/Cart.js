import React, { Component } from "react";
import { View, Text } from "react-native";

export default class Cart extends Component {
    static navigationOptions = {
        title: "Cart"
    };

    render() {
        return <Text>Cart View</Text>
    }
}